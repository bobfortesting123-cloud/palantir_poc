policy:
  approval:
    - "approved by a write-permission holder"
approval_rules:
  - name: "approved by a write-permission holder"
    requires:
      count: 1
      permissions:
        - write
        - admin
    options:
      methods:
        github_review: true
